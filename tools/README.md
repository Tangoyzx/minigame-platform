# 🛠️ 微信小游戏上传工具

这是一个自动化上传微信小游戏的工具，可以帮你一键上传游戏代码到微信平台！

## 📋 准备工作

### 1. 获取 AppID

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入你的小游戏
3. 在 **开发管理 -> 开发设置** 中找到 AppID

### 2. 下载代码上传密钥

1. 登录微信公众平台
2. 进入 **开发管理 -> 开发设置**
3. 找到 **小程序代码上传**
4. 点击 **下载** 代码上传密钥
5. 将下载的文件重命名为 `private.key`
6. 将文件放到 `tools` 目录下

### 3. 配置 IP 白名单

1. 登录微信公众平台
2. 进入 **开发管理 -> 开发设置**
3. 找到 **小程序代码上传 -> IP 白名单**
4. 添加你当前电脑的公网 IP 地址
   - 可以在 [ip138.com](https://www.ip138.com/) 查看你的公网 IP

### 4. 修改配置

打开 `upload.js` 文件，修改以下配置：

```javascript
const config = {
  // 改成你的真实 AppID
  appid: 'wx1234567890abcdef',
  
  // 版本号（每次上传需要更新）
  version: '1.0.0',
  
  // 版本描述
  desc: '小游戏集合平台 - 自动上传'
};
```

## 🚀 使用方法

### 安装依赖

首次使用需要安装依赖：

```bash
cd tools
npm install
```

### 上传代码

```bash
npm run upload
```

上传成功后：
1. 登录微信公众平台
2. 进入 **版本管理 -> 开发版本**
3. 可以选择提交审核或设为体验版

### 生成预览二维码

```bash
npm run upload:preview
```

生成后会在 `tools` 目录下产生 `preview-qrcode.jpg` 文件，用微信扫描即可预览。

## ❓ 常见问题

### Q: 上传时提示 IP 不在白名单？

A: 请检查你的公网 IP 是否已添加到微信公众平台的 IP 白名单。注意公网 IP 可能会变化，建议定期检查。

### Q: 提示找不到密钥文件？

A: 请确保：
1. 密钥文件已下载
2. 文件名为 `private.key`
3. 文件放在 `tools` 目录下

### Q: 上传后在哪里查看？

A: 登录微信公众平台 -> 版本管理 -> 开发版本，可以看到刚上传的版本。

## 📁 目录结构

```
tools/
├── upload.js        # 上传脚本
├── package.json     # 依赖配置
├── private.key      # 代码上传密钥（需自己下载）
├── preview-qrcode.jpg  # 预览二维码（自动生成）
└── README.md        # 本文件
```

## 🔗 相关链接

- [微信公众平台](https://mp.weixin.qq.com/)
- [miniprogram-ci 官方文档](https://developers.weixin.qq.com/miniprogram/dev/devtools/ci.html)
- [微信小游戏开发文档](https://developers.weixin.qq.com/minigame/dev/guide/)
