# 云服务器开发快速配置指南

这个文档教你如何在云服务器上使用Gemini CLI继续开发这个微信小游戏项目。

## 📦 准备工作

### 1. 上传项目到云服务器

使用 `scp` 或其他方式上传整个项目文件夹:
```bash
# 示例: 使用scp上传
scp -r minigame-platform/ user@your-server:/path/to/projects/
```

**重要**: 确保上传了所有文件,特别是:
- `.codebuddy/` 文件夹 (包含AI规则)
- `docs/` 文件夹 (包含项目文档)
- `game/` 文件夹 (游戏代码)

### 2. 安装Gemini CLI

参考Google官方文档安装Gemini CLI工具。

## 🚀 首次使用Gemini时的设置

### 方法1: 使用初始化提示词

在云服务器上首次运行Gemini CLI时,使用这个提示词:

```
你好!我需要你帮我开发一个微信小游戏项目。

在开始之前,请先阅读以下文件来了解项目规则:

1. 阅读 .codebuddy/rules/ 文件夹中的所有 .mdc 文件
   - 这些文件定义了项目的核心规则
   
2. 阅读 docs/README.md
   - 了解项目整体架构和功能
   
3. 阅读 docs/AI-Development-Guide.md
   - 这是专门为AI助手准备的开发指南

这些规则非常重要,请在后续的每次对话中都遵守!

特别提醒:
- 这是微信小游戏项目,所有代码必须符合微信小游戏规范
- 你的角色是游戏开发导师,要用易懂的语言解释技术
- 每次回复前要说"你好!!"
- 修改代码后要及时更新文档

现在请先告诉我你是否已经成功读取了这些规则文件。
```

### 方法2: 创建配置文件

你也可以创建一个配置文件,每次启动时自动加载:

在项目根目录创建 `.gemini-config.txt`:
```bash
cd /path/to/minigame-platform
cat > .gemini-config.txt << 'EOF'
项目类型: 微信小游戏
主要语言: JavaScript

必须遵守的规则:
1. 你是游戏开发导师,对象是技术较弱的小孩子
2. 每次回复前说"你好!!"
3. 用易懂的语言解释技术点
4. 代码必须符合微信小游戏规范
5. 修改代码后立即更新 docs/ 中的文档

重要文件:
- .codebuddy/rules/ - AI规则配置
- docs/README.md - 项目文档
- docs/AI-Development-Guide.md - AI开发指南
- game/js/base/BaseGame.js - 游戏基类

在开始任何任务前,请先阅读相关文档!
EOF
```

然后每次使用Gemini时,先让它读取这个配置:
```bash
gemini "请先阅读 .gemini-config.txt 和相关文档,然后帮我[具体任务]"
```

## 💡 日常使用技巧

### 任务提示词模板

每次执行新任务时,使用这个模板:

```
【任务】: [具体描述你要做什么]

【上下文】:
- 这是微信小游戏项目
- 请先查看 docs/ 中的相关文档
- 如有需要,阅读现有代码了解实现

【要求】:
- 代码必须符合微信小游戏规范
- 用易懂的语言解释你的改动
- 修改后更新文档
- 回复前说"你好!!"

【示例任务】:
比如: "添加一个新的打砖块游戏"
比如: "修复FlappyBird的碰撞检测bug"
比如: "优化游戏大厅的UI"
```

### 快速命令示例

```bash
# 添加新游戏
gemini "
【任务】: 添加一个打砖块(Breakout)游戏

【要求】:
- 继承BaseGame基类
- 使用项目的物理系统
- 在游戏大厅添加入口
- 更新docs/README.md

请先阅读 docs/AI-Development-Guide.md 了解开发流程
"

# 修复bug
gemini "
【任务】: FlappyBird游戏中小鸟有时会穿过管道

【上下文】:
- 游戏文件在 game/js/games/flappybird/
- 物理系统在 game/js/physics/
- 请先查看这些文件

【要求】: 找到并修复碰撞检测问题,用易懂的语言解释原因
"

# 优化代码
gemini "
【任务】: 优化游戏大厅的性能

【上下文】:
- 游戏大厅代码在 game/js/scenes/GameLobby.js
- 请先阅读代码了解现有实现

【要求】: 提出优化建议并实施,解释为什么这样做更好
"
```

## 🔄 与本地开发同步

### 从云服务器下载修改

```bash
# 下载修改后的代码
scp -r user@your-server:/path/to/minigame-platform ./

# 或者使用git同步
cd minigame-platform
git add .
git commit -m "云服务器开发的新功能"
git push
```

### 上传本地修改到云服务器

```bash
# 上传本地修改
scp -r minigame-platform/ user@your-server:/path/to/projects/

# 或者在云服务器上git pull
ssh user@your-server
cd /path/to/minigame-platform
git pull
```

## ⚠️ 注意事项

### 1. 文件同步
- 确保 `.codebuddy/` 文件夹始终同步
- `docs/` 文件夹的修改要及时同步
- 不要在云服务器上删除这些关键文件夹

### 2. 规则遵守
- 每次新会话开始时,提醒Gemini阅读规则
- 如果Gemini的回复不符合规则,及时纠正它
- 保持文档的及时更新

### 3. 测试
- 云服务器上只能编写代码,无法运行微信小游戏
- 需要定期同步到本地,在微信开发者工具中测试
- 建议使用git进行版本管理

## 📋 检查清单

每次在云服务器上开发前:
- [ ] 确认项目文件完整(特别是 `.codebuddy/` 和 `docs/`)
- [ ] 让Gemini读取了项目规则
- [ ] 了解当前要做的任务

每次开发完成后:
- [ ] 检查代码是否符合微信小游戏规范
- [ ] 更新了相关文档
- [ ] 同步代码到本地测试
- [ ] 提交git (如果使用版本控制)

## 🎯 目标

通过这个配置,你可以:
- ✅ 在云服务器上高效使用Gemini CLI开发
- ✅ 保持项目规则的一致性
- ✅ 方便地与本地环境同步
- ✅ 享受学习游戏开发的乐趣

---

**开始你的云端开发之旅吧! ☁️**
